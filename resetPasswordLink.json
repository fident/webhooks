{
  "name": "resetPasswordLink",
  "type": "object",
  "description": "A reset password link for a user",
  "properties": {
    "identity": {
      "type": "string",
      "description": "ID for the identity requesting a login link"
    },
    "resetLink": {
      "type": "string",
      "description": "Link for the user to click to login to their account, and reset their password (Single Use)"
    }
  }
}
